{
  "name": "backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@hiteshgarg/medium-common": "^1.0.1",
    "@prisma/client": "^6.13.0",
    "@prisma/extension-accelerate": "^2.0.2",
    "bcrypt-edge": "^0.1.1",
    "hono": "^4.8.5",
    "prisma": "^6.13.0"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
